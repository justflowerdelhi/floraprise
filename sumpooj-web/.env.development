# Dev environment — API calls go through the Vite proxy (/api → http://localhost:5148)
# This avoids CORS preflight failures entirely (same-origin from browser's perspective).
VITE_API_BASE_URL=/api
VITE_DEV_BYPASS_AUTH=false
VITE_POS_MAX_DISCOUNT_PERCENT=30
VITE_POS_MAX_DISCOUNT_AMOUNT=5000
